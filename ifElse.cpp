/* if-else are two conditional statements, which are generally
used when we wnat to run the code based on some condition*/
#include<iostream>
using namespace std;

int main()
{
    int number;
    cout << "Enter a number: ";
    cin >> number;

    if(number == 10)
        cout << "You got it";
    else
        cout << "Not this one";
}
